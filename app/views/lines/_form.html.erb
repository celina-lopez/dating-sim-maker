<%= f.input :order %>
<!-- hide order -->
<%= f.input(
  :speakeable_id,
  as: :select,
  collection: [
    ["MC", 0],
    *current_user.characters.map{ |c| [c.name, c.id] }
  ]
)%>
<%= f.input :text %>
<%= f.input(
  :emotion,
  as: :select,
  collection: Line.emotions.keys.map {|k| [k.humanize, k]},
)%>

<!-- show preview? -->
<!-- add required -->

<h1>New Dating Sim</h1>
<%= simple_form_for(@story, html: { 'data-parsley-validate': true}) do |f| %>
  <%= render "form", f: f, story: @story %>
  <div class="form-actions pb-2">
    <%= f.button :submit, class: 'btn-dating' %>
  </div>
<% end %>
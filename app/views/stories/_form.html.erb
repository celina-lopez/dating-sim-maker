<%= f.input :title, required: true %>
<%= f.input :description %>
<%= f.input(
  :status,
  as: :select,
  collection: Story.statuses.keys.map {|k| [k.humanize, k]},
  required: true,
  input_html: {
    class: "w-50"
  }
)%>
